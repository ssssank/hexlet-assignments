ru:
  activerecord:
    models:
      # BEGIN
      post: Пост
      # END
      post/comment: Комментарий

    attributes:
      post:
        # BEGIN
        title: Заголовок
        body: Содержание
        # END
      post/comment:
        body: Содержание

    errors:
      models:
        post:
          attributes:
            # BEGIN
            title:
              blank: can't be blank
            body:
              blank: can't be blank
            # END
